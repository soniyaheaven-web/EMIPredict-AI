# EMIPredict-AI
EMIPredict AI is a machine learning web app built with Streamlit that predicts EMI eligibility and maximum EMI amount. Uses XGBoost (98% accuracy) for classification and Random Forest (R²: 96.43%) for regression. Trained on 400K records with MLflow tracking.
